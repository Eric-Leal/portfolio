export { createClient } from './client'
export { createClient as createServerClient } from './server'

/**
 * Nota: `createClient` é a versão para Client Components (browser).
 * `createServerClient` é a versão para Server Components / route handlers.
 */