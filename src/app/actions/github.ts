// Este arquivo é mantido como ponto de entrada para futuras Server Actions
// (ex: formulário de contato, guestbook).
// Dados do GitHub são buscados diretamente em src/lib/github.ts pelos Server Components.
export {}
